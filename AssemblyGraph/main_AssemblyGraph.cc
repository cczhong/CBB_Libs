#include <iostream>

#include "AssemblyGraph.h"

using namespace std;

int main()  {
    AssemblyGraph graph;
    cout << "is graph initialized:  " << graph.IsInitialized() << endl;

    return 0;
}